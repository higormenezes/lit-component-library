import React from "react"
import { createComponent } from "@lit/react"
import { IconButton as _IconButton } from "./IconButton"

export const IconButton = createComponent({
  tagName: "wc-icon-button",
  elementClass: _IconButton,
  react: React,
  events: {},
})
