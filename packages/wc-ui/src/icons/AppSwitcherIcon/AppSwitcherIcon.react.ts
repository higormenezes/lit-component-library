import React from "react"
import { createComponent } from "@lit/react"
import { AppSwitcherIcon as _AppSwitcherIcon } from "./AppSwitcherIcon"

export const AppSwitcherIcon = createComponent({
  tagName: "wc-app-switcher-icon",
  elementClass: _AppSwitcherIcon,
  react: React,
  events: {},
})
